import React from 'react';
import styles from './AddressesPage.module.scss';

export const AddressesPage: React.FC = () => {
  return (
    <section className="addresses">
      <h2 className={styles.title}>Адреса площадок для уличного спорта</h2>
    </section>
  );
};
